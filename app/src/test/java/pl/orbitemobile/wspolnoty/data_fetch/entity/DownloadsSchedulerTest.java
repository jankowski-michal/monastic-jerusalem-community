/*
 * Copyright (c) 2017. All Rights Reserved. Michal Jankowski orbitemobile.pl
 */

package pl.orbitemobile.wspolnoty.data_fetch.entity;


import org.junit.Test;

public class DownloadsSchedulerTest {

    private String url = "";


    @Test
    public void testDataResponseCreation_valid() throws Exception {
/*        mDownladsScheduler = new DownloadsScheduler();
        mDownladsScheduler.setmTaskManager(new MockedTaskManager());
        Assert.assertEquals(mDownladsScheduler.queueLength(), 0);
        DownloadTask task = DownloadTask.Builder.shortEvent(url);
        DownloadTask task2 = DownloadTask.Builder.shortEvent(url);
        DownloadTask task3 = DownloadTask.Builder.shortEvent(url);
        mDownladsScheduler.add(task);
        Assert.assertEquals(mDownladsScheduler.queueLength(), 1);
        mDownladsScheduler.add(task2);
        mDownladsScheduler.add(task3);
        Assert.assertEquals(mDownladsScheduler.queueLength(), 3);
        mDownladsScheduler.finished(task);
        Assert.assertEquals(mDownladsScheduler.queueLength(), 2);
        mDownladsScheduler.finished(task2);
        DownloadTask task4 = DownloadTask.Builder.shortEvent(url);
        mDownladsScheduler.add(task4);
        Assert.assertEquals(mDownladsScheduler.queueLength(), 2);
        mDownladsScheduler.finished(task3);
        Assert.assertEquals(mDownladsScheduler.queueLength(), 1);
        mDownladsScheduler.finished(task4);
        Assert.assertEquals(mDownladsScheduler.queueLength(), 0);*/
    }
    
}
